export type MessageModel = {
  id: {
    creationTime: string
    increment: number
    machine: number
    pid: number
    timestamp: number
  }
  userName: string
  content: string
  createdAt: string
  roomName: string
}
